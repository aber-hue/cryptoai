# 加密货币交易所数据获取技术方案

## 专家组成员

本方案由以下虚拟专家组共同设计：

1. **API架构师** - 负责设计多层次数据获取架构
2. **数据工程师** - 负责数据清洗和标准化流程
3. **安全专家** - 负责反爬虫对策和风险评估
4. **DevOps工程师** - 负责稳定性和监控方案
5. **产品经理** - 负责需求分析和优先级排序

---

## 一、需求分析

### 1.1 核心数据需求

根据产品功能，系统需要获取以下三类核心数据：

#### 数据类型1：交易所新上代币/新上合约
**所需字段：**
- 代币符号（Symbol）
- 代币全称（Name）
- 代币图标（Logo）
- 上线时间（Listing Time）
- 上线交易所（Exchange）
- 交易类型（Spot/Perpetual/Futures/Options）
- 交易对（Trading Pairs）
- 上线价格（Initial Price）

**更新频率：** 实时监控（每5-10分钟检查一次）

#### 数据类型2：公告信息
**所需字段：**
- 公告标题（Title）
- 公告类型（Type：上币公告/活动公告）
- 交易所（Exchange）
- 发布时间（Publish Time）
- 公告内容（Content）
- 相关代币（Related Tokens）
- 活动时间（Event Time）
- 奖励信息（Rewards）

**更新频率：** 每10-30分钟检查一次

#### 数据类型3：代币详情信息
**所需字段：**
- 基本信息：价格、市值、交易量、供应量
- 价格历史：历史价格数据（日K线）
- 市场深度：买卖盘深度数据
- 持仓信息：各交易所的持仓量、资金费率
- 解锁计划：代币解锁时间表
- 项目信息：官网、白皮书、社交媒体、合约地址

**更新频率：** 
- 价格/交易量：每1-5分钟
- 深度数据：每10分钟
- 其他信息：每小时或每天

---

## 二、数据源分析

### 2.1 官方交易所API

#### Binance API
**优势：**
- 官方支持，数据准确可靠
- 提供REST和WebSocket接口
- 覆盖Spot、Margin、Futures、Options全市场
- 有明确的速率限制和文档

**限制：**
- 速率限制：根据权重计算，一般为1200请求/分钟
- 需要API Key（部分端点）
- IP限制（某些地区可能需要代理）

**可用端点：**
- `/api/v3/exchangeInfo` - 获取交易对信息
- `/api/v3/ticker/24hr` - 获取24小时价格数据
- `/api/v3/klines` - 获取K线数据
- `/api/v3/depth` - 获取深度数据
- 公告页面：需要爬虫获取（无官方API）

#### OKX API
**优势：**
- 提供公告API（`/api/v5/support/announcements`）
- 公开端点不需要认证
- 速率限制明确（每2秒5-20次请求）
- 支持获取所有交易类型的工具列表

**限制：**
- 速率限制相对较低
- 公开端点响应基于IP受限

**可用端点：**
- `/api/v5/support/announcements` - 获取公告
- `/api/v5/support/announcement-types` - 获取公告类型
- `/api/v5/public/instruments` - 获取交易工具列表
- `/api/v5/market/ticker` - 获取行情数据
- `/api/v5/market/books` - 获取深度数据

#### Bybit API
**优势：**
- 支持v5版本API，功能完整
- 提供Futures、Options、Spot交易数据
- 速度快，延迟低

**限制：**
- 公告信息需要爬虫获取
- 速率限制需要注意

#### 其他交易所（Gate、Kucoin、Bitget、Coinbase、Upbit、Bithumb）
**现状：**
- 大部分提供基础的市场数据API
- 公告信息通常需要爬虫获取
- 速率限制和文档质量参差不齐

### 2.2 第三方数据聚合服务

#### CoinGecko API
**优势：**
- 覆盖18,000+代币
- 提供1年历史数据（免费Demo计划）
- 支持DEX数据（250+区块链网络，1,700+ DEX，15M+代币）
- 免费计划：每月10,000次调用

**限制：**
- 免费计划有调用次数限制
- 数据更新延迟（通常5-10分钟）
- 不提供实时WebSocket

**定价：**
- Demo计划：免费（10,000次/月）
- Analyst计划：需付费（具体价格需查询）

**适用场景：**
- 代币基本信息获取
- 历史价格数据
- 市值排名数据
- 作为主要数据源的补充

#### CoinMarketCap API
**优势：**
- 权威的加密货币数据平台
- 数据覆盖全面
- 免费计划：10,000次调用/月，11个市场数据端点

**限制：**
- 免费计划功能有限
- 付费计划价格较高（Startup: $79/月）

**定价：**
- Basic计划：免费（10,000次/月，11端点）
- Startup计划：$79/月（14端点，包含历史数据）
- Standard计划：更高价格

**适用场景：**
- 代币排名和市值数据
- 作为备用数据源
- 数据交叉验证

### 2.3 网页爬虫

**适用场景：**
- 交易所公告页面（Binance、Bybit等无公告API的交易所）
- 活动信息页面
- 代币项目官网信息

**技术选型：**
- Puppeteer/Playwright：适合动态网页
- Cheerio：适合静态网页
- Axios + JSDOM：轻量级方案

**挑战：**
- 反爬虫机制
- 页面结构变化
- IP封禁风险
- 数据提取准确性

---

## 三、多层次数据获取架构

### 3.1 架构设计原则

1. **分层设计**：主数据源 + 备用数据源 + 补充数据源
2. **容错机制**：单一数据源失败不影响整体系统
3. **数据验证**：多数据源交叉验证，确保准确性
4. **缓存策略**：减少API调用，提高响应速度
5. **增量更新**：只获取变化的数据，节省资源

### 3.2 数据获取层级

#### 第一层：主数据源（官方API）
**职责：** 获取实时、准确的核心数据

**数据源：**
- Binance API：Binance相关数据
- OKX API：OKX相关数据（包括公告）
- Bybit API：Bybit相关数据
- 其他交易所官方API

**获取策略：**
- 使用官方REST API获取市场数据
- 使用WebSocket获取实时价格更新
- 定时轮询获取新上币信息

**速率控制：**
- 为每个交易所设置独立的速率限制器
- 使用令牌桶算法控制请求频率
- 优先级队列：重要数据优先获取

#### 第二层：备用数据源（第三方聚合API）
**职责：** 当主数据源失败时提供备用数据，或补充主数据源缺失的信息

**数据源：**
- CoinGecko API：代币基本信息、历史数据
- CoinMarketCap API：市值排名、备用价格数据

**获取策略：**
- 定时同步代币基本信息（每小时）
- 主数据源失败时自动切换
- 用于数据交叉验证

**成本控制：**
- 使用免费计划（每月10,000次调用）
- 合理分配调用额度
- 缓存数据，减少重复调用

#### 第三层：补充数据源（网页爬虫）
**职责：** 获取官方API和第三方API无法提供的数据

**目标网站：**
- Binance公告页面：https://www.binance.com/en/support/announcement
- Bybit公告页面
- 其他交易所的公告和活动页面

**获取策略：**
- 使用Puppeteer进行动态网页爬取
- 定时任务（每10-30分钟）
- 检测页面变化，只爬取新内容

**反爬虫对策：**
- 使用代理IP池轮换
- 模拟真实浏览器行为
- 随机化请求间隔
- 设置合理的User-Agent

### 3.3 数据流转流程

```
[交易所官方API] ──┐
[第三方聚合API] ──┼──> [数据获取层] ──> [数据清洗层] ──> [数据标准化层] ──> [数据存储层] ──> [API服务层] ──> [前端展示]
[网页爬虫]      ──┘
```

**详细流程：**

1. **数据获取层**
   - 定时任务调度器触发数据获取
   - 根据优先级选择数据源
   - 并发获取多个交易所的数据
   - 错误处理和重试机制

2. **数据清洗层**
   - 去除重复数据
   - 过滤无效数据
   - 修正异常值
   - 补全缺失字段

3. **数据标准化层**
   - 统一字段命名
   - 统一时间格式（UTC时间戳）
   - 统一价格单位（USD）
   - 统一数据结构

4. **数据存储层**
   - 写入数据库（MySQL/PostgreSQL）
   - 更新缓存（Redis）
   - 记录变更日志

5. **API服务层**
   - 提供RESTful API
   - 实现数据查询和过滤
   - 返回标准化的JSON数据

6. **前端展示**
   - 从API服务层获取数据
   - 渲染用户界面
   - 实时更新（WebSocket）

---

## 四、数据清洗和标准化方案

### 4.1 数据清洗流程

#### 步骤1：数据验证
**验证规则：**
- 必填字段检查：symbol、name、exchange、listingTime不能为空
- 数据类型检查：price必须为数字，time必须为有效时间戳
- 数值范围检查：price > 0，volume >= 0
- 格式检查：symbol必须为大写字母，exchange必须在预定义列表中

**处理策略：**
- 验证失败的数据记录到错误日志
- 可修复的错误自动修正（如symbol转大写）
- 无法修复的数据丢弃，并发送告警

#### 步骤2：去重处理
**去重维度：**
- 代币去重：基于 (symbol + exchange + listingTime) 唯一标识
- 公告去重：基于 (exchange + publishTime + title) 唯一标识
- 价格数据去重：基于 (symbol + exchange + timestamp) 唯一标识

**去重策略：**
- 使用数据库唯一索引防止重复插入
- 内存中维护布隆过滤器快速判断
- 保留最新数据，丢弃旧数据

#### 步骤3：数据补全
**补全策略：**
- 代币图标：从CoinGecko API获取
- 代币全称：从CoinMarketCap API获取
- 合约地址：从区块链浏览器获取
- 项目官网：从代币官方渠道获取

**补全优先级：**
1. 从缓存中查找
2. 从第三方API获取
3. 从爬虫数据中提取
4. 标记为"未知"，等待人工补全

#### 步骤4：异常值处理
**异常检测：**
- 价格异常：与历史价格偏离超过50%
- 交易量异常：突然暴涨或暴跌超过10倍
- 时间异常：未来时间或过去时间超过合理范围

**处理策略：**
- 标记为"待审核"
- 从多个数据源交叉验证
- 人工审核确认后再发布

### 4.2 数据标准化规范

#### 字段命名规范
**统一命名：**
- 使用camelCase命名法：`tokenSymbol`, `listingTime`, `tradingVolume`
- 避免缩写：使用`exchange`而不是`ex`
- 保持一致性：所有时间字段以`Time`结尾

#### 时间格式标准化
**统一格式：**
- 存储：Unix时间戳（毫秒）
- 传输：ISO 8601格式（`2025-01-30T12:00:00Z`）
- 时区：统一使用UTC时区

**示例：**
```json
{
  "listingTime": 1738238400000,
  "listingTimeISO": "2025-01-30T12:00:00Z"
}
```

#### 价格和数量标准化
**统一单位：**
- 价格：统一使用USD（美元）
- 数量：使用科学计数法表示大数值
- 精度：价格保留8位小数，数量保留2位小数

**示例：**
```json
{
  "price": 50000.12345678,
  "volume": 1234567890.12,
  "marketCap": 1.23e12
}
```

#### 交易所标识标准化
**统一标识：**
- 使用小写字母：`binance`, `okx`, `bybit`
- 交易类型后缀：`binance_spot`, `binance_perps`, `okx_futures`

**映射表：**
```json
{
  "Binance": "binance",
  "OKX": "okx",
  "Bybit": "bybit",
  "Gate.io": "gate",
  "KuCoin": "kucoin",
  "Bitget": "bitget",
  "Coinbase": "coinbase",
  "Upbit": "upbit",
  "Bithumb": "bithumb"
}
```

#### 数据结构标准化
**统一JSON结构：**

**代币上线数据：**
```json
{
  "id": "uuid",
  "tokenSymbol": "BTC",
  "tokenName": "Bitcoin",
  "tokenLogo": "https://cdn.example.com/btc.png",
  "exchange": "binance",
  "exchangeLogo": "https://cdn.example.com/binance.png",
  "tradingType": "spot",
  "listingTime": 1738238400000,
  "listingPrice": 50000.12345678,
  "currentPrice": 51000.00000000,
  "priceChange24h": 2.0,
  "tradingPairs": ["BTC/USDT", "BTC/BUSD"],
  "volume24h": 1234567890.12,
  "createdAt": 1738238400000,
  "updatedAt": 1738238400000
}
```

**公告数据：**
```json
{
  "id": "uuid",
  "exchange": "okx",
  "exchangeLogo": "https://cdn.example.com/okx.png",
  "announcementType": "listing",
  "title": "OKX will list XYZ token",
  "content": "Full announcement content...",
  "relatedTokens": ["XYZ"],
  "publishTime": 1738238400000,
  "eventTime": 1738324800000,
  "sourceUrl": "https://www.okx.com/support/announcement/...",
  "createdAt": 1738238400000,
  "updatedAt": 1738238400000
}
```

**代币详情数据：**
```json
{
  "tokenSymbol": "BTC",
  "tokenName": "Bitcoin",
  "tokenLogo": "https://cdn.example.com/btc.png",
  "currentPrice": 51000.00000000,
  "priceChange24h": 2.0,
  "marketCap": 1000000000000.00,
  "fullyDilutedValuation": 1100000000000.00,
  "volume24h": 50000000000.00,
  "circulatingSupply": 19000000.00,
  "totalSupply": 21000000.00,
  "maxSupply": 21000000.00,
  "ath": 69000.00000000,
  "atl": 100.00000000,
  "website": "https://bitcoin.org",
  "whitepaper": "https://bitcoin.org/bitcoin.pdf",
  "socials": {
    "twitter": "https://twitter.com/bitcoin",
    "telegram": "https://t.me/bitcoin",
    "discord": "https://discord.gg/bitcoin"
  },
  "contracts": [
    {
      "chain": "ethereum",
      "address": "0x..."
    }
  ],
  "exchanges": [
    {
      "exchange": "binance",
      "tradingPairs": ["BTC/USDT"],
      "volume24h": 10000000000.00
    }
  ],
  "updatedAt": 1738238400000
}
```

---

## 五、风险评估和反爬虫对策

### 5.1 主要风险

#### 风险1：API速率限制
**描述：** 超过交易所API的速率限制导致请求被拒绝或IP被封禁

**影响：** 数据获取中断，系统无法正常工作

**概率：** 中等

**应对策略：**
- 实现速率限制器，严格控制请求频率
- 使用多个API Key轮换
- 实现指数退避重试机制
- 监控API调用次数，提前预警

#### 风险2：数据源不稳定
**描述：** 交易所API或网站临时不可用、响应超时

**影响：** 数据缺失或延迟

**概率：** 中等

**应对策略：**
- 多数据源冗余设计
- 自动故障转移机制
- 数据缓存，使用历史数据填补空白
- 健康检查和告警系统

#### 风险3：反爬虫机制
**描述：** 网站检测到爬虫行为，封禁IP或返回错误数据

**影响：** 无法获取公告等关键数据

**概率：** 高

**应对策略：**
- 使用代理IP池
- 模拟真实浏览器行为
- 降低爬取频率
- 使用验证码识别服务（如需要）

#### 风险4：数据准确性问题
**描述：** 不同数据源的数据不一致，或数据本身有误

**影响：** 向用户展示错误信息，损害产品信誉

**概率：** 中等

**应对策略：**
- 多数据源交叉验证
- 异常值检测和人工审核
- 用户反馈机制
- 数据版本控制和回滚

#### 风险5：成本超支
**描述：** 第三方API调用次数超过免费额度，或代理IP成本过高

**影响：** 运营成本增加

**概率：** 中等

**应对策略：**
- 合理分配API调用额度
- 优化缓存策略，减少重复调用
- 监控成本，设置预算告警
- 优先使用免费数据源

### 5.2 反爬虫对策详解

#### 对策1：代理IP池
**实现方案：**
- 购买或租用代理IP服务（如Bright Data、Oxylabs）
- 维护一个IP池，每次请求随机选择IP
- 检测IP是否被封禁，自动移除失效IP
- 定期补充新的IP

**成本：**
- 免费方案：使用公共代理（不稳定）
- 付费方案：$50-200/月（取决于IP数量和质量）

**推荐服务：**
- Bright Data（原Luminati）
- Oxylabs
- SmartProxy

#### 对策2：浏览器指纹模拟
**实现方案：**
- 使用Puppeteer/Playwright模拟真实浏览器
- 随机化User-Agent、屏幕分辨率、语言等
- 模拟鼠标移动、滚动等人类行为
- 加载图片和CSS，避免被检测为无头浏览器

**技术栈：**
- Puppeteer + puppeteer-extra-plugin-stealth
- Playwright
- Selenium（较重，不推荐）

#### 对策3：请求频率控制
**实现方案：**
- 设置随机延迟（1-5秒）
- 避免在短时间内大量请求同一页面
- 模拟真实用户的浏览模式（先访问首页，再访问详情页）

**示例代码：**
```javascript
async function fetchWithDelay(url) {
  const delay = Math.random() * 4000 + 1000; // 1-5秒随机延迟
  await new Promise(resolve => setTimeout(resolve, delay));
  return await fetch(url);
}
```

#### 对策4：验证码处理
**实现方案：**
- 使用验证码识别服务（如2Captcha、Anti-Captcha）
- 对于简单验证码，使用OCR技术自动识别
- 对于复杂验证码（如reCAPTCHA），使用人工打码服务

**成本：**
- 2Captcha：$2.99/1000次识别
- Anti-Captcha：$0.5-3/1000次识别（取决于验证码类型）

#### 对策5：Cookie和Session管理
**实现方案：**
- 保持Cookie和Session的持久化
- 模拟登录后的用户行为（如需要）
- 定期更新Cookie，避免过期

**注意事项：**
- 不要频繁清除Cookie
- 保持Session的连续性

### 5.3 监控和告警

#### 监控指标
- API调用成功率
- 数据获取延迟
- 数据质量（异常值比例）
- 爬虫成功率
- IP封禁次数
- 成本消耗

#### 告警规则
- API调用失败率超过10%
- 数据获取延迟超过5分钟
- 爬虫连续失败3次
- 成本超过预算的80%

#### 告警渠道
- 邮件通知
- Slack/钉钉/企业微信
- 短信通知（紧急情况）

---

## 六、技术实现方案

### 6.1 技术栈选型

#### 后端
- **语言：** Node.js (TypeScript)
- **框架：** Express.js / Fastify
- **数据库：** PostgreSQL（关系型数据） + Redis（缓存）
- **任务调度：** node-cron / Bull（基于Redis的队列）
- **HTTP客户端：** Axios / node-fetch
- **WebSocket客户端：** ws
- **爬虫：** Puppeteer / Playwright

#### 数据处理
- **数据验证：** Joi / Zod
- **数据转换：** Lodash
- **日期处理：** Day.js

#### 监控和日志
- **日志：** Winston / Pino
- **监控：** Prometheus + Grafana
- **告警：** Alertmanager

#### 部署
- **容器化：** Docker
- **编排：** Docker Compose / Kubernetes
- **CI/CD：** GitHub Actions

### 6.2 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                       调度层 (Scheduler)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 实时数据任务  │  │ 定时数据任务  │  │ 爬虫任务      │      │
│  │ (每1-5分钟)  │  │ (每小时/天)   │  │ (每10-30分钟) │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    数据获取层 (Data Fetcher)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 交易所API     │  │ 第三方API     │  │ 网页爬虫      │      │
│  │ Connector    │  │ Connector    │  │ Scraper      │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                  数据处理层 (Data Processor)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 数据验证      │  │ 数据清洗      │  │ 数据标准化    │      │
│  │ Validator    │  │ Cleaner      │  │ Normalizer   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层 (Data Storage)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ PostgreSQL   │  │ Redis Cache  │  │ 文件存储      │      │
│  │ (主数据库)    │  │ (缓存)       │  │ (日志/备份)   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    API服务层 (API Service)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ REST API     │  │ WebSocket    │  │ GraphQL      │      │
│  │              │  │ (实时推送)    │  │ (可选)       │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      前端应用 (Frontend)                     │
│                  加密货币交易所上币跟踪系统                   │
└─────────────────────────────────────────────────────────────┘
```

### 6.3 核心模块设计

#### 模块1：数据获取调度器 (DataFetchScheduler)
**职责：**
- 管理所有数据获取任务
- 根据优先级和频率调度任务
- 监控任务执行状态

**核心功能：**
```typescript
class DataFetchScheduler {
  // 注册定时任务
  registerTask(taskName: string, cronExpression: string, handler: Function)
  
  // 立即执行任务
  executeTask(taskName: string)
  
  // 暂停/恢复任务
  pauseTask(taskName: string)
  resumeTask(taskName: string)
  
  // 获取任务状态
  getTaskStatus(taskName: string): TaskStatus
}
```

#### 模块2：交易所API连接器 (ExchangeAPIConnector)
**职责：**
- 封装各交易所的API调用
- 处理认证和签名
- 实现速率限制

**核心功能：**
```typescript
class ExchangeAPIConnector {
  // 获取交易对信息
  async getInstruments(exchange: string): Promise<Instrument[]>
  
  // 获取行情数据
  async getTicker(exchange: string, symbol: string): Promise<Ticker>
  
  // 获取深度数据
  async getOrderBook(exchange: string, symbol: string): Promise<OrderBook>
  
  // 获取K线数据
  async getKlines(exchange: string, symbol: string, interval: string): Promise<Kline[]>
  
  // 获取公告（如支持）
  async getAnnouncements(exchange: string): Promise<Announcement[]>
}
```

#### 模块3：数据清洗器 (DataCleaner)
**职责：**
- 验证数据完整性
- 去除重复数据
- 修正异常值

**核心功能：**
```typescript
class DataCleaner {
  // 验证数据
  validate(data: any, schema: Schema): ValidationResult
  
  // 去重
  deduplicate(dataList: any[], uniqueKeys: string[]): any[]
  
  // 补全缺失字段
  fillMissingFields(data: any, defaults: any): any
  
  // 检测异常值
  detectAnomalies(data: any, rules: AnomalyRule[]): Anomaly[]
}
```

#### 模块4：数据标准化器 (DataNormalizer)
**职责：**
- 统一数据格式
- 转换字段命名
- 标准化时间和价格

**核心功能：**
```typescript
class DataNormalizer {
  // 标准化代币上线数据
  normalizeListingData(rawData: any, exchange: string): ListingData
  
  // 标准化公告数据
  normalizeAnnouncementData(rawData: any, exchange: string): AnnouncementData
  
  // 标准化代币详情数据
  normalizeTokenData(rawData: any): TokenData
  
  // 时间格式转换
  normalizeTime(time: any): number
  
  // 价格格式转换
  normalizePrice(price: any): number
}
```

#### 模块5：网页爬虫 (WebScraper)
**职责：**
- 爬取交易所公告页面
- 提取结构化数据
- 处理反爬虫机制

**核心功能：**
```typescript
class WebScraper {
  // 爬取公告列表
  async scrapeAnnouncements(exchange: string): Promise<RawAnnouncement[]>
  
  // 爬取公告详情
  async scrapeAnnouncementDetail(url: string): Promise<RawAnnouncementDetail>
  
  // 使用代理
  setProxy(proxyUrl: string)
  
  // 设置延迟
  setDelay(minDelay: number, maxDelay: number)
}
```

### 6.4 数据库设计

#### 表1：tokens（代币表）
```sql
CREATE TABLE tokens (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  symbol VARCHAR(20) NOT NULL,
  name VARCHAR(100) NOT NULL,
  logo_url TEXT,
  website TEXT,
  whitepaper TEXT,
  socials JSONB,
  contracts JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(symbol)
);
```

#### 表2：listings（上币记录表）
```sql
CREATE TABLE listings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  token_symbol VARCHAR(20) NOT NULL,
  exchange VARCHAR(50) NOT NULL,
  trading_type VARCHAR(20) NOT NULL,
  listing_time BIGINT NOT NULL,
  listing_price DECIMAL(20, 8),
  trading_pairs JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(token_symbol, exchange, trading_type, listing_time)
);
```

#### 表3：announcements（公告表）
```sql
CREATE TABLE announcements (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  exchange VARCHAR(50) NOT NULL,
  announcement_type VARCHAR(50) NOT NULL,
  title TEXT NOT NULL,
  content TEXT,
  related_tokens JSONB,
  publish_time BIGINT NOT NULL,
  event_time BIGINT,
  source_url TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(exchange, publish_time, title)
);
```

#### 表4：token_prices（价格历史表）
```sql
CREATE TABLE token_prices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  token_symbol VARCHAR(20) NOT NULL,
  exchange VARCHAR(50) NOT NULL,
  price DECIMAL(20, 8) NOT NULL,
  volume_24h DECIMAL(20, 2),
  timestamp BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(token_symbol, exchange, timestamp)
);
CREATE INDEX idx_token_prices_symbol_time ON token_prices(token_symbol, timestamp);
```

#### 表5：market_depth（市场深度表）
```sql
CREATE TABLE market_depth (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  token_symbol VARCHAR(20) NOT NULL,
  exchange VARCHAR(50) NOT NULL,
  bid_depth_2pct DECIMAL(20, 2),
  ask_depth_2pct DECIMAL(20, 2),
  total_depth DECIMAL(20, 2),
  timestamp BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(token_symbol, exchange, timestamp)
);
CREATE INDEX idx_market_depth_symbol_time ON market_depth(token_symbol, timestamp);
```

---

## 七、实施路线图

### 阶段1：基础设施搭建（第1-2周）
**目标：** 搭建基础开发环境和数据库

**任务：**
- [ ] 搭建开发环境（Node.js + TypeScript）
- [ ] 设计并创建数据库表结构
- [ ] 搭建Redis缓存服务
- [ ] 配置日志和监控系统
- [ ] 编写基础工具类（速率限制器、重试机制等）

**交付物：**
- 完整的开发环境
- 数据库Schema
- 基础工具库

### 阶段2：主数据源接入（第3-4周）
**目标：** 接入主要交易所的官方API

**任务：**
- [ ] 实现Binance API连接器
- [ ] 实现OKX API连接器
- [ ] 实现Bybit API连接器
- [ ] 实现其他交易所API连接器
- [ ] 编写单元测试

**交付物：**
- 各交易所API连接器
- 测试报告

### 阶段3：数据清洗和标准化（第5周）
**目标：** 实现数据清洗和标准化流程

**任务：**
- [ ] 实现数据验证模块
- [ ] 实现去重模块
- [ ] 实现数据标准化模块
- [ ] 实现异常值检测模块
- [ ] 编写单元测试

**交付物：**
- 数据处理模块
- 测试报告

### 阶段4：备用数据源接入（第6周）
**目标：** 接入第三方数据聚合API

**任务：**
- [ ] 实现CoinGecko API连接器
- [ ] 实现CoinMarketCap API连接器
- [ ] 实现数据源切换逻辑
- [ ] 实现数据交叉验证
- [ ] 编写单元测试

**交付物：**
- 第三方API连接器
- 多数据源容错机制

### 阶段5：网页爬虫开发（第7-8周）
**目标：** 实现公告页面爬虫

**任务：**
- [ ] 实现Binance公告爬虫
- [ ] 实现Bybit公告爬虫
- [ ] 实现其他交易所公告爬虫
- [ ] 实现代理IP池管理
- [ ] 实现反爬虫对策
- [ ] 编写单元测试

**交付物：**
- 公告爬虫模块
- 反爬虫策略实现

### 阶段6：任务调度和自动化（第9周）
**目标：** 实现自动化数据获取和调度

**任务：**
- [ ] 实现任务调度器
- [ ] 配置定时任务（实时数据、定时数据、爬虫任务）
- [ ] 实现任务监控和告警
- [ ] 实现失败重试机制
- [ ] 编写单元测试

**交付物：**
- 任务调度系统
- 监控和告警系统

### 阶段7：API服务开发（第10周）
**目标：** 提供数据查询API

**任务：**
- [ ] 设计RESTful API接口
- [ ] 实现代币上线查询API
- [ ] 实现公告查询API
- [ ] 实现代币详情查询API
- [ ] 实现缓存策略
- [ ] 编写API文档

**交付物：**
- RESTful API服务
- API文档

### 阶段8：测试和优化（第11-12周）
**目标：** 全面测试和性能优化

**任务：**
- [ ] 集成测试
- [ ] 压力测试
- [ ] 性能优化（数据库查询、缓存策略）
- [ ] 修复Bug
- [ ] 编写运维文档

**交付物：**
- 测试报告
- 性能优化报告
- 运维文档

### 阶段9：上线和监控（第13周）
**目标：** 部署到生产环境并持续监控

**任务：**
- [ ] 部署到生产环境
- [ ] 配置监控和告警
- [ ] 数据质量检查
- [ ] 建立运维流程
- [ ] 准备应急预案

**交付物：**
- 生产环境部署
- 监控仪表盘
- 运维手册

---

## 八、成本估算

### 8.1 开发成本
- **人力成本：** 1名全栈工程师 × 3个月 = 约$15,000-30,000（取决于地区和经验）
- **服务器成本：** 云服务器（2核4G）× 3个月 = 约$100-200
- **总计：** 约$15,100-30,200

### 8.2 运营成本（每月）
- **服务器：** $50-100（云服务器）
- **数据库：** $20-50（托管数据库）
- **Redis：** $10-30（托管Redis）
- **代理IP：** $50-200（取决于使用量）
- **第三方API：** $0-79（CoinGecko免费，CoinMarketCap可选付费）
- **验证码识别：** $0-50（按需使用）
- **监控和日志：** $0-30（部分服务免费）
- **总计：** 约$130-539/月

### 8.3 成本优化建议
1. **优先使用免费数据源**：充分利用CoinGecko和CoinMarketCap的免费计划
2. **优化API调用**：实现缓存策略，减少重复调用
3. **按需使用代理IP**：只在必要时使用代理，降低成本
4. **自建服务器**：如果长期运营，考虑自建服务器降低成本
5. **开源工具**：优先使用开源的监控和日志工具

---

## 九、总结和建议

### 9.1 方案总结

本方案通过**多层次数据获取架构**，结合**官方API、第三方聚合API和网页爬虫**三种数据源，实现了稳定、可靠的数据获取。通过**数据清洗和标准化流程**，确保数据质量和一致性。通过**反爬虫对策和监控告警**，降低风险并保证系统稳定运行。

**核心优势：**
1. **多数据源冗余**：单一数据源失败不影响整体系统
2. **数据质量保证**：多数据源交叉验证，异常值检测
3. **稳定性高**：速率限制、重试机制、故障转移
4. **可扩展性强**：模块化设计，易于添加新的交易所或数据源
5. **成本可控**：优先使用免费数据源，按需使用付费服务

### 9.2 实施建议

#### 短期建议（第1-3个月）
1. **优先接入主要交易所API**：Binance、OKX、Bybit
2. **实现基础的数据清洗和标准化**
3. **开发简单的公告爬虫**（Binance、OKX）
4. **搭建监控和告警系统**

#### 中期建议（第4-6个月）
1. **接入更多交易所**：Gate、Kucoin、Bitget、Coinbase、Upbit、Bithumb
2. **优化数据清洗流程**，提高数据质量
3. **实现更复杂的反爬虫对策**
4. **接入第三方数据聚合API**作为备用数据源

#### 长期建议（第7-12个月）
1. **实现实时WebSocket数据推送**
2. **开发数据分析和预测功能**
3. **建立用户反馈机制**，持续改进数据质量
4. **探索区块链数据源**，获取链上数据

### 9.3 风险提示

1. **法律风险**：爬虫行为可能违反网站的服务条款，需谨慎评估
2. **技术风险**：反爬虫机制可能随时升级，需持续维护
3. **成本风险**：代理IP和第三方API成本可能超出预算
4. **数据风险**：数据源可能提供错误数据，需建立验证机制

### 9.4 最终建议

**推荐方案：**
- **第一优先级**：使用官方API（Binance、OKX、Bybit等）
- **第二优先级**：使用第三方聚合API（CoinGecko、CoinMarketCap）作为补充
- **第三优先级**：使用网页爬虫获取公告信息（仅在必要时使用）

**关键成功因素：**
1. **稳定性优先**：确保系统7×24小时稳定运行
2. **数据质量优先**：宁可数据少，也要保证准确
3. **成本可控**：合理分配资源，避免成本超支
4. **持续优化**：根据实际运行情况不断优化和改进

---

## 附录

### 附录A：主要交易所API文档链接
- Binance API: https://binance-docs.github.io/apidocs/spot/en/
- OKX API: https://www.okx.com/docs-v5/en/
- Bybit API: https://bybit-exchange.github.io/docs/
- Gate.io API: https://www.gate.io/docs/developers/apiv4/
- KuCoin API: https://docs.kucoin.com/
- Bitget API: https://bitgetlimited.github.io/apidoc/en/spot/
- Coinbase API: https://docs.cloud.coinbase.com/exchange/docs
- Upbit API: https://docs.upbit.com/
- Bithumb API: https://apidocs.bithumb.com/

### 附录B：第三方数据服务
- CoinGecko API: https://docs.coingecko.com/
- CoinMarketCap API: https://coinmarketcap.com/api/
- CryptoCompare API: https://min-api.cryptocompare.com/
- Messari API: https://messari.io/api

### 附录C：代理IP服务
- Bright Data: https://brightdata.com/
- Oxylabs: https://oxylabs.io/
- SmartProxy: https://smartproxy.com/
- ProxyMesh: https://proxymesh.com/

### 附录D：验证码识别服务
- 2Captcha: https://2captcha.com/
- Anti-Captcha: https://anti-captcha.com/
- CapSolver: https://www.capsolver.com/

---

**文档版本：** 1.0  
**创建日期：** 2025-01-30  
**最后更新：** 2025-01-30  
**作者：** 数据获取专家组
